<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:background="#232323">

    <ImageView
        android:id="@+id/imgSuccess"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/successful"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"/>

    <TextView
        android:id="@+id/txtSuccess"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ACCOUNT CREATED"
        android:layout_below="@id/imgSuccess"
        android:layout_marginTop="8dp"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:textColor="#fff"/>

    <TextView
        android:id="@+id/txtStatement"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your account has been successfully created and from now onwards you can continue to order medicines from your nearest pharmacy store."
        android:layout_below="@id/txtSuccess"
        android:layout_marginTop="8dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:textSize="18dp"
        android:textAlignment="center"
        android:textColor="#fff"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        android:layout_below="@id/txtStatement"
        android:layout_marginBottom="10dp"
        android:orientation="horizontal">

        <android.widget.Button
            android:id="@+id/btnFreeTrial"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/btn_bg_new"
            android:text="Proceed to buy medicines"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:textColor="#000"/>

    </LinearLayout>

</RelativeLayout>